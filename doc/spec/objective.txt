v2(2020-9-25)

新バージョンの主な目的、目標

    OS,実行環境に依存しないモジュール提供
        Linux,Windows どちらでも利用できるようにする
        シングルバイナリによるモジュール配布
    検査対象への負荷を極力抑える。現行処理時間の 90% 削減
        Go言語、低レベルなAPIライブラリに変更
        エージェントによるコマンド実行
    第三者でも違和感なく判るレポートの提供
        値の意味、値が変更されている箇所が容易に分かるようにする
        サマリから詳細へ順に値を追えるようにする
